ITERATION PROMPT - FIX U-DOST TOPIC UNDERSTANDING:
Keep all existing features unchanged. Only fix U-Dost to understand specific topics correctly:
CRITICAL FIX - U-DOST NOT UNDERSTANDING SPECIFIC TOPICS
CURRENT PROBLEM:

User asks for "Islam" definition → Gets general Islamiyat guidance ❌
U-Dost not recognizing the actual topic word
Giving course overview instead of specific topic response

REQUIRED FIX:
1. FIX TOPIC RECOGNITION SYSTEM
javascriptCopyfunction generateSpecificResponse() {
    const topic = document.getElementById('topicInput').value.trim().toLowerCase();
    
    if (!topic) {
        alert('Please enter a topic!');
        return;
    }
    
    console.log(`User entered topic: "${topic}"`); // Debug log
    console.log(`Menu: ${currentUserSelection.menuChoice}, Grade: ${currentUserSelection.grade}, Subject: ${currentUserSelection.subject}`);
    
    // DIRECT TOPIC MATCHING - Fix this part
    const specificResponse = generateTopicSpecificContent(
        currentUserSelection.menuChoice,
        currentUserSelection.grade, 
        currentUserSelection.subject,
        topic  // Use the actual topic, not generic subject
    );
    
    displayUDostResponse(specificResponse);
}

function generateTopicSpecificContent(menuChoice, grade, subject, topic) {
    console.log(`Generating ${menuChoice} for topic: "${topic}"`);
    
    // DEFINITIONS - SPECIFIC TOPIC RESPONSES
    if (menuChoice === 'Definitions') {
        return generateDefinitionForTopic(grade, subject, topic);
    } else if (menuChoice === 'Activities') {
        return generateActivitiesForTopic(grade, subject, topic);
    } else if (menuChoice === 'Teaching Strategies') {
        return generateStrategiesForTopic(grade, subject, topic);
    }
    
    return `Error: Could not generate content for topic "${topic}"`;
}
2. CREATE SPECIFIC DEFINITION RESPONSES
javascriptCopyfunction generateDefinitionForTopic(grade, subject, topic) {
    // ISLAMIYAT TOPIC DEFINITIONS
    if (subject === 'Islamiyat') {
        
        if (topic.includes('islam') || topic === 'islam') {
            return generateIslamDefinition(grade);
        } else if (topic.includes('allah')) {
            return generateAllahDefinition(grade);
        } else if (topic.includes('prophet') || topic.includes('muhammad')) {
            return generateProphetDefinition(grade);
        } else if (topic.includes('quran')) {
            return generateQuranDefinition(grade);
        } else if (topic.includes('namaz') || topic.includes('prayer')) {
            return generateNamazDefinition(grade);
        }
    }
    
    // ENGLISH TOPIC DEFINITIONS  
    if (subject === 'English') {
        if (topic.includes('noun')) {
            return generateNounDefinition(grade);
        } else if (topic.includes('verb')) {
            return generateVerbDefinition(grade);
        }
    }
    
    // MATH TOPIC DEFINITIONS
    if (subject === 'Math') {
        if (topic.includes('addition') || topic.includes('add')) {
            return generateAdditionDefinition(grade);
        } else if (topic.includes('subtraction') || topic.includes('subtract')) {
            return generateSubtractionDefinition(grade);
        }
    }
    
    // FALLBACK - If topic not recognized
    return `Hello teacher! "${topic}" ke definition ke liye Grade ${grade} ${subject} mein specific content generate kar rahi hun...

📖 DEFINITION - ${topic} (Grade ${grade} ${subject})

Main is topic ko recognize kar rahi hun aur appropriate definition banayungi. Thoda wait kariye teacher! 😊`;
}

function generateIslamDefinition(grade) {
    if (grade === '1' || grade === '2') {
        return `Hello teacher! Grade ${grade} ke liye "Islam" definition:

📖 DEFINITION - Islam (Grade ${grade} Islamiyat)

🔹 ONE LINE:
English: Islam is our beautiful religion that teaches us to worship Allah.
Roman Urdu: Islam hamara khoobsurat mazhab hai jo hame Allah ki ibadat sikhata hai.

🔹 TWO LINE:
English: Islam is our religion. It teaches us to be good and worship only Allah.
Roman Urdu: Islam hamara mazhab hai. Yeh hame acha banne aur sirf Allah ki ibadat karne ka kehta hai.

🔹 THREE LINE:  
English: Islam is our peaceful religion that was brought by Prophet Muhammad (PBUH). It teaches us to worship only Allah and be kind to everyone. Muslims follow Islam all around the world.
Roman Urdu: Islam hamara aman wala mazhab hai jo Hazrat Muhammad (PBUH) laye. Yeh hame sirf Allah ki ibadat aur sab ke saath achay se rehne sikhata hai. Duniya bhar mein Muslim Islam follow karte hain.

Samajh aa gaya teacher? 😊`;
    }
    
    if (grade === '3') {
        return `Hello teacher! Grade 3 ke liye "Islam" definition:

📖 DEFINITION - Islam (Grade 3 Islamiyat)

🔹 DETAILED DEFINITION:
English: Islam is the religion of peace and submission to Allah. It has five pillars and teaches us the right way to live.
Roman Urdu: Islam Allah ke samne jhukne aur aman ka mazhab hai. Iske paanch arkan hain aur yeh hame sahi tarike se jeene sikhata hai.

Aur detail chahiye teacher? 😊`;
    }
    
    return `Grade ${grade} ke liye Islam definition ready kar rahi hun teacher!`;
}

function generateAllahDefinition(grade) {
    return `Hello teacher! Grade ${grade} ke liye "Allah" definition:

📖 DEFINITION - Allah (Grade ${grade} Islamiyat)

🔹 ONE LINE:
English: Allah is our Creator and the only God we worship.
Roman Urdu: Allah hamare banane wale hain aur hum sirf unki ibadat karte hain.

🔹 DETAILED:
English: Allah is the one and only God who created everything. He has no partners and is the most merciful and kind.
Roman Urdu: Allah wahid khuda hain jinhone sab kuch banaya. Unka koi shareek nahi aur woh bohat mehrbaan aur raham wale hain.

Perfect hai teacher? 😊`;
}

function generateNounDefinition(grade) {
    return `Hello teacher! Grade ${grade} English ke liye "Noun" definition:

📖 DEFINITION - Noun (Grade ${grade} English)

🔹 ONE LINE:
English: A noun is a word that names a person, place, or thing.
Roman Urdu: Noun aik word hai jo kisi insaan, jagah ya cheez ka naam batata hai.

🔹 WITH EXAMPLES:
English: A noun names people (Ahmed, teacher), places (school, Pakistan), or things (book, chair).
Roman Urdu: Noun logo (Ahmed, teacher), jagahon (school, Pakistan), ya cheezon (kitab, chair) ke naam batata hai.

Theek hai teacher? 😊`;
}
3. FIX MENU CHOICE RECOGNITION
javascriptCopy// Make sure menu choices are recognized correctly
document.querySelectorAll('.menu-option').forEach(option => {
    option.addEventListener('click', function() {
        const menuText = this.textContent.trim();
        
        // Clean menu choice recognition
        if (menuText.includes('Definition') || menuText.includes('📖')) {
            currentUserSelection.menuChoice = 'Definitions';
        } else if (menuText.includes('Activities') || menuText.includes('🎮')) {
            currentUserSelection.menuChoice = 'Activities';  
        } else if (menuText.includes('Strategies') || menuText.includes('🎯')) {
            currentUserSelection.menuChoice = 'Teaching Strategies';
        } else if (menuText.includes('Assessment') || menuText.includes('📝')) {
            currentUserSelection.menuChoice = 'Assessments';
        } else if (menuText.includes('Lesson') || menuText.includes('📚')) {
            currentUserSelection.menuChoice = 'Lesson Plans';
        }
        
        console.log(`Menu choice set to: ${currentUserSelection.menuChoice}`);
        showGradeSelection();
    });
});
TEST CASES:

Definitions → Grade 2 → Islamiyat → "Islam" → Should give Islam definition for Grade 2
Definitions → Grade 1 → English → "noun" → Should give noun definition for Grade 1
Activities → Grade 3 → Math → "addition" → Should give addition activities for Grade 3

RESULT: U-Dost will understand exact topics and give specific responses, not generic course overviews.
Keep all other app features unchanged.