SIMPLE RESPONSIVE CSS FIX:
Keep everything same. Only add mobile-friendly styles:
ADD RESPONSIVE CSS
cssCopy/* Mobile First Responsive Design */
@media screen and (max-width: 768px) {
    
    /* General Mobile Fixes */
    body {
        font-size: 16px;
        padding: 10px;
    }
    
    /* Buttons */
    button {
        min-height: 44px;
        font-size: 16px;
        padding: 12px 16px;
        margin: 8px 4px;
    }
    
    /* Input Fields */
    input, select, textarea {
        min-height: 44px;
        font-size: 16px;
        padding: 12px;
        width: 100%;
        box-sizing: border-box;
    }
    
    /* Navigation */
    .nav-button {
        display: block;
        width: 100%;
        margin: 5px 0;
    }
    
    /* Dashboard Cards */
    .feature-cards {
        flex-direction: column;
        gap: 10px;
    }
    
    /* Planner */
    .day-schedule {
        margin: 10px 0;
        padding: 15px;
    }
    
    .period-row {
        flex-direction: column;
        gap: 8px;
    }
    
    .calendar-grid {
        grid-template-columns: repeat(auto-fit, minmax(80px, 1fr));
        gap: 5px;
    }
    
    /* Class Management */
    .class-card {
        width: 100%;
        margin: 10px 0;
    }
    
    .students-grid {
        grid-template-columns: 1fr;
    }
    
    /* Today's Sidebar - Move to Bottom on Mobile */
    .todays-schedule {
        position: fixed !important;
        bottom: 0 !important;
        right: 0 !important;
        top: auto !important;
        width: 100% !important;
        max-height: 200px;
        overflow-y: auto;
    }
    
    /* Zoom Controls */
    .zoom-controls {
        top: 5px !important;
        right: 5px !important;
        font-size: 14px;
    }
    
    /* Forms */
    .form-group {
        margin: 15px 0;
    }
    
    .form-actions {
        flex-direction: column;
        gap: 10px;
    }
    
    /* Tables */
    table {
        font-size: 14px;
    }
    
    .scores-table {
        overflow-x: auto;
    }
    
    /* Upload Areas */
    .upload-areas {
        flex-direction: column;
        gap: 15px;
    }
    
    /* Modal/Popup Adjustments */
    .modal, .popup {
        width: 95vw !important;
        height: auto !important;
        max-height: 90vh;
        overflow-y: auto;
    }
}

/* Tablet View */
@media screen and (min-width: 769px) and (max-width: 1024px) {
    
    .calendar-grid {
        grid-template-columns: repeat(5, 1fr);
    }
    
    .upload-areas {
        flex-direction: row;
    }
    
    .todays-schedule {
        right: 10px !important;
        width: 200px !important;
    }
}

/* Desktop View */
@media screen and (min-width: 1025px) {
    .todays-schedule {
        right: 20px !important;
        width: 250px !important;
    }
}

/* Touch Friendly */
@media (hover: none) and (pointer: coarse) {
    button {
        min-height: 48px;
        font-size: 16px;
    }
    
    input, select {
        min-height: 48px;
    }
    
    .calendar-day {
        min-height: 60px;
    }
}
Add this CSS to make everything mobile-friendly. Don't change any JavaScript or functionality.Add to Conversation